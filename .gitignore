node_modules/
.DS_Store

# Build intermediates (slides.pdf, *.webp는 청중 배포용으로 commit)
presentation/slides.html
presentation/slides.pptx
presentation/assets/diagrams/*.png

# Claude Code 런타임 / 개인 환경
.claude/hook-runs.log
.claude/.hook-lock
.claude/scheduled_tasks.lock
.claude/settings.local.json

# 발표자 전용 (외부 노출 금지)
init.md
decisions.md
presentation/speaker-notes.md
presentation/drafts/

# 라이브 데모 작업 상태 (account ID 포함, 로컬 전용)
tmp/
# 데모 evidence 의 raw(원본) 사이드카 — sanitize 전 버전, commit 금지
presentation/assets/demos/**/*.raw

# Terraform 데모 (state·provider 캐시는 로컬 전용)
scripts/terraform-trust-limit-demo/.terraform/
scripts/terraform-trust-limit-demo/.terraform.lock.hcl
scripts/terraform-trust-limit-demo/terraform.tfstate
scripts/terraform-trust-limit-demo/terraform.tfstate.backup
scripts/terraform-trust-limit-demo/*.tfvars

# Editor / OS
*.swp
*~
.idea/
.vscode/
